#ifndef TEXTSPACK_H
#define TEXTSPACK_H
#include <SFML/Graphics.hpp>


class TextsPack
{
    public:
        TextsPack();
        virtual ~TextsPack();

        void setTextPosition(sf::Text &t, sf::Vector2f pos);
        sf::Text getLightLabelText();
        sf::Text getErrorTitleText();
        sf::Texture getConnectionErrorText();

    protected:

    private:
        sf::Font font;
        sf::Text lightLabel;
        sf::Text errorTitle;
        sf::Text connectionError;
};

#endif // TEXTSPACK_H
